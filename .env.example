PORT=3003
NODE_ENV=development
SESSION_SECRET=change-me-to-a-random-string
CORS_ORIGIN=http://localhost:3002
# DATABASE_URL is optional. When unset, the server resolves the DB path from
# its own module location: packages/server/data/wargame.sqlite (works from any CWD).
# Set this only if you want to point at a different file (e.g. a test fixture).
# DATABASE_URL=./packages/server/data/wargame.sqlite
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=mistral:7b-instruct-v0.3-q4_K_M
AI_ENABLED=false
